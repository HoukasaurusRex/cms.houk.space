{
  "kind": "collectionType",
  "connection": "default",
  "collectionName": "links",
  "info": {
    "name": "Links"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "link": {
      "type": "string"
    },
    "category": {
      "type": "enumeration",
      "enum": [
        "tool",
        "blog",
        "design"
      ]
    },
    "screenshots": {
      "collection": "file",
      "via": "related",
      "plugin": "upload",
      "required": false
    },
    "title": {
      "type": "string"
    }
  }
}
