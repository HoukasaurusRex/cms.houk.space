{
  "kind": "collectionType",
  "connection": "default",
  "collectionName": "links",
  "info": {
    "name": "Links"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "Link": {
      "type": "string"
    },
    "Category": {
      "type": "enumeration",
      "enum": [
        "tool",
        "blog",
        "design"
      ]
    },
    "Screenshots": {
      "collection": "file",
      "via": "related",
      "plugin": "upload",
      "required": false
    }
  }
}
